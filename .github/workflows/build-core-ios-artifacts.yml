name: Build Shared iOS Artifacts

on:
  workflow_dispatch:
  push:
    branches: [kmp]
    paths:
      - "build.gradle.kts"
      - "settings.gradle.kts"
      - "gradle/**"
      - "build-logic/**"
      - "core/common/**"
      - "core/data/**"
      - "core/database/**"
      - "core/datastore/**"
      - "core/datastore-proto/**"
      - "core/network/**"
      - ".github/workflows/build-core-ios-artifacts.yml"
  pull_request:
    branches: [kmp]
    paths:
      - "build.gradle.kts"
      - "settings.gradle.kts"
      - "gradle/**"
      - "build-logic/**"
      - "core/common/**"
      - "core/data/**"
      - "core/database/**"
      - "core/datastore/**"
      - "core/datastore-proto/**"
      - "core/network/**"
      - ".github/workflows/build-core-ios-artifacts.yml"

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build-ios-core-artifacts:
    runs-on: macos-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          distribution: zulu
          java-version: "21"

      - name: Setup Gradle
        uses: gradle/gradle-build-action@v3

      - name: Grant execute permission for gradlew
        run: chmod +x ./gradlew

      - name: Build Shared iOS Artifacts
        run: ./gradlew assembleSharedIosArtifacts --stacktrace --info

      - name: Upload Shared iOS Artifacts
        uses: actions/upload-artifact@v4
        with:
          name: shared-ios-artifacts
          path: shared/build/XCFrameworks/release/ASShared.xcframework
          if-no-files-found: error
